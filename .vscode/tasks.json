{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build ESP32",
      "type": "shell",
      "command": "cargo build",
      "options": {
        "cwd": "${workspaceFolder}/esp" // Specify the working directory here
      },
      "problemMatcher": ["$rustc"]
    }
  ]
}